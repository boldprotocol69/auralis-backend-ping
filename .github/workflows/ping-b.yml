name: Ping B

on:
  schedule:
    - cron: "2-59/6 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - run: curl -s https://auralis-c6ne.onrender.com/health > /dev/null
